# Metroskool Monitor — Windows attendance shell
# Copy to `.env` (gitignored). Never put service-role keys in this app.

VITE_MONITOR_URL=http://localhost:3004/monitor
VITE_ADMIN_URL=http://localhost:3000
