# metroskool_flutter_core

Shared Dart types for Metroskool mobile modules (`SchoolId`, `ModuleId`,
`SyncQueue`). Vendored in this Monitor repo so `mobile/` can resolve
`package:metroskool_flutter_core` without the suite root at
`../../packages/flutter_core`.
